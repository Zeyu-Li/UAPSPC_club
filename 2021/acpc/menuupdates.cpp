#include <bits/stdc++.h>

typedef long long ll;
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int d, u;
    cin >> d >> u;
    int max, curr;
    max = curr = 0;
    vector<int> items;
    while (u--) {
        char a;
        cin >> a;
        if (a == 'a') {
            cout << 
        } else {
            // add to menu
            int k;
            cin >> k;
        }
    }
    return 0;
}
